<% if !user_signed_in? %>
<div class="splash">
  <h1>Welcome to Your Expenses Name</h1>
  <p>A brief description of your app.</p>
  <div class="splash-buttons">
    <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary' %>
    <%= link_to 'Log In', new_user_session_path, class: 'btn btn-secondary' %>
  </div>
</div>
<% else%>
    <h1>Here is your home page <%= current_user.name%> 😊</h1>
    <p> Would you like to log out?  <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
<% end %>
